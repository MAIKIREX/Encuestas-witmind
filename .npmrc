; Node no confia por defecto en el CA que inyecta el proxy/antivirus TLS de esta
; maquina, lo que rompe las llamadas del servidor a Supabase con
; UNABLE_TO_VERIFY_LEAF_SIGNATURE. Esto hace que Node use el almacen del sistema.
node-options=--use-system-ca
